#!/usr/bin/expect
set timeout 60
spawn ssh -o StrictHostKeyChecking=no dmrhospi@162.214.80.27 "cd public_html/hr && unzip -o vendor.zip && php artisan storage:link && php artisan optimize:clear"
expect "password:"
send {1012AK$$007ss@}
send "\r"
expect eof
